nelmio_api_doc:
    documentation:
        info:
            title: Brakolu
            description: Brakolu est l'API pour la brasserie.
            version: 0.0.1
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc

    models: { use_jms: true }