import { OrderDetailCreation } from './order-detail-creation';

describe('OrderDetailCreation', () => {
  it('should create an instance', () => {
    expect(new OrderDetailCreation()).toBeTruthy();
  });
});
