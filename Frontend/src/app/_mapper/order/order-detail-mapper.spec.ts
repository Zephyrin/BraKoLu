import { OrderDetailMapper } from './order-detail-mapper';

describe('OrderDetailMapper', () => {
  it('should create an instance', () => {
    expect(new OrderDetailMapper()).toBeTruthy();
  });
});
